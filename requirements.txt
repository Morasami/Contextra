# Contextra MCP Memory Server - Core Dependencies
# For lightweight, native-first setup with MCP stdio server

# MCP Protocol
mcp>=1.0.0

# Database (Lightweight)
sqlalchemy>=2.0.0

# Vector Database (Lightweight)
chromadb>=0.4.0

# Embeddings (Local)
sentence-transformers>=2.2.0
numpy>=1.24.0

# Data Validation
pydantic>=2.5.0

# Configuration
python-dotenv>=1.0.0

# HTTP client for embeddings
httpx>=0.25.0
